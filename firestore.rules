rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read for catalog and public content across all devices
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
